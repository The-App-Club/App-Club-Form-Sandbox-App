import clsx from 'clsx'

import Form from '@/features/min-max/components/Form'

const MinMax = () => {
  return (
    <div className={clsx(`mx-auto mt-12 w-full max-w-md`)}>
      <Form />
    </div>
  )
}

export default MinMax
