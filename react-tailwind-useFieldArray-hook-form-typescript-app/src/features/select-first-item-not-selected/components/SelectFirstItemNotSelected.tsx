import clsx from 'clsx'

import Form from '@/features/select-first-item-not-selected/components/Form'

const Sample = () => {
  return (
    <div className={clsx(`mx-auto mt-12 w-full max-w-md`)}>
      <Form />
    </div>
  )
}

export default Sample
