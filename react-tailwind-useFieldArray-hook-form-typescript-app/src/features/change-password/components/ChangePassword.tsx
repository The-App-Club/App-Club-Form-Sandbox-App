import clsx from 'clsx'

import Form from '@/features/change-password/components/Form'

const ChangePassword = () => {
  return (
    <div className={clsx(`mx-auto mt-12 w-full max-w-md`)}>
      <Form />
    </div>
  )
}

export default ChangePassword
